extern int ObjObjCollisions(mesh* one, mesh* two);
extern int backupObjObjCollisions(mesh* one, mesh* two);
extern int EntObjCollisions(mesh* M, ent* E);
extern int backupEntObjCollisions(mesh* M, ent* E);
